# Qket 팀 프로젝트 위키 — 운영 규칙

이 파일은 Claude Code가 이 위키를 어떻게 유지보수하는지 정의한다.
새 세션을 시작할 때마다 이 파일을 먼저 읽고, `wiki/index.md`와 `wiki/log.md`를 확인한 뒤 작업을 시작한다.

## 목적

Qket(공연 예매 시스템) 프로젝트를 진행하며 팀원 여럿이 공유하는 지식 베이스.
개인 학습용이 아니라 **프로젝트 개발 중에 실제로 필요한 컨벤션·구조·결정·트러블슈팅**을 담는다. 여러 팀원이 각자의 Claude Code 세션에서 이 위키를 읽고 쓰므로, 한 사람의 머릿속에만 있던 맥락(왜 이렇게 짰는지, 어떤 버그를 겪었는지)이 팀 전체 자산으로 남도록 유지한다.

이 저장소는 git으로 관리되며 `frontend`/`backend`/`Infra`와 동등한 위치의 별도 저장소다. 위키 갱신도 그 저장소들처럼 commit → push하고, 팀원은 세션 시작 전 pull 받는 걸 전제로 한다.

---

## 디렉토리 구조

```
CLAUDE_LLM/
├── CLAUDE.md              # 이 파일 — 운영 규칙
├── wiki/
│   ├── index.md           # 전체 페이지 목록 (카테고리별 정렬)
│   ├── log.md              # 작업 이력 (append-only, 작성자 포함)
│   ├── conventions/        # "우리는 이렇게 코딩한다" — 규칙
│   ├── architecture/       # "시스템이 이렇게 동작한다" — 구조 설명
│   ├── decisions/          # ADR — 왜 이렇게 하기로 했는가 (대안/트레이드오프 포함)
│   ├── troubleshooting/    # 실제 겪은 버그와 해결책 (postmortem)
│   ├── runbook/            # 반복되는 운영 절차 (DB 마이그레이션, 배포 등)
│   └── onboarding/         # 신규 팀원 진입 가이드
```

각 카테고리의 성격:

| 카테고리 | 질문 | 예시 |
|---|---|---|
| `conventions/` | 새 코드를 어떻게 짜야 하는가? | 주석 형식, API 응답 규격, 네이밍 |
| `architecture/` | 시스템이 지금 어떻게 되어있는가? | 레이어 구조, 인증 흐름, 패키지 경계 |
| `decisions/` | 왜 이렇게 하기로 했는가? | 특정 설계를 고른 이유와 버린 대안 |
| `troubleshooting/` | 어떤 버그를 겪었고 어떻게 고쳤는가? | 인코딩 버그, null 처리 버그 |
| `runbook/` | 반복 작업을 어떤 절차로 하는가? | 로컬 DB 스키마 변경 |
| `onboarding/` | 새 팀원이 뭘 먼저 봐야 하는가? | 체크리스트, 필독 페이지 링크 |

---

## 페이지 형식

### 모든 페이지 공통 frontmatter

```yaml
---
title: 페이지 제목
category: conventions | architecture | decisions | troubleshooting | runbook | onboarding
tags: [태그1, 태그2]
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

### conventions/ 페이지 구조

```markdown
# 제목

## 규칙
(구체적으로, 예외 케이스까지)

## 왜 이렇게 하나
(이유 없는 규칙은 안 지켜진다)

## 관련
- [[연관페이지]]
```

### architecture/ 페이지 구조

```markdown
# 제목

## 구조 / 요약

## 의도적으로 어색하거나 특이한 지점
(신규 팀원이 "이거 버그 아닌가" 오해할 만한 부분을 미리 설명)

## 관련
- [[연관페이지]]
```

### decisions/ 페이지 구조 (ADR)

```markdown
---
status: 확정 | 논의중 | 폐기
date: YYYY-MM-DD
author: 이름
---

# 결정 제목

## 배경
## 결정
## 고려한 대안
## 트레이드오프 / 남은 리스크
## 관련
```

### troubleshooting/ 페이지 구조

```markdown
# 제목

## 증상
## 원인
## 해결
## 재발 방지
## 관련
```

### runbook/ 페이지 구조

```markdown
# 제목

## 배경 (필요 시)
## 절차
## 주의사항
## 관련
```

---

## 운영 워크플로우

일반 개인 위키의 "소스 문서를 읽고 정리"가 아니라, **PR 머지 / 버그 수정 / 팀 결정** 시점에 갱신되는 것이 이 위키의 핵심 트리거다.

### 1. 새 컨벤션이 정해졌을 때

팀이 코딩 규칙을 새로 정하거나 바꾸면:

1. `wiki/conventions/`에 페이지 작성 또는 기존 페이지 갱신
2. 관련 `architecture/`·`troubleshooting/` 페이지에 상호 링크 추가
3. `wiki/index.md` 갱신
4. `wiki/log.md`에 항목 추가: `## [YYYY-MM-DD] 작성자 | ingest | 컨벤션명`

### 2. 버그를 겪고 해결했을 때

1. `wiki/troubleshooting/`에 증상/원인/해결/재발방지 페이지 작성
2. 그 버그의 원인이 된 컨벤션이 있다면 `conventions/`에도 "왜 이 규칙이 있는지" 링크로 반영
3. `log.md`에 `troubleshooting` 항목 추가

### 3. 설계 결정을 내렸을 때

1. `wiki/decisions/`에 ADR 작성 (배경/결정/대안/트레이드오프)
2. `log.md`에 `decision` 항목 추가

### 4. 질의 응답

사용자가 질문을 하면:

1. `wiki/index.md`에서 관련 페이지 탐색
2. 관련 페이지들을 읽고 종합해서 답변 (출처 페이지 링크 포함)
3. 코드베이스와 위키 내용이 어긋나 보이면(코드가 바뀌었는데 위키가 안 바뀐 경우) 사용자에게 알리고 갱신 제안

### 5. 위키 점검 (lint)

사용자가 요청하면:

- 고아 페이지(인바운드 링크 없음) 탐색
- 코드베이스와 모순되는 내용 탐색 (코드가 더 최신일 가능성이 높음 — 실제 파일을 확인하고 위키를 갱신)
- 링크는 되어 있지만 페이지가 없는 개념 탐색
- `decisions/`에 기록 안 된 채 프로즈로만 남아있는 설계 근거가 있는지 확인

---

## 링크 규칙

- 페이지 간 링크: `[[페이지명]]` (Obsidian 위키링크 형식)
- 링크는 아낌없이 사용한다. 없는 페이지를 링크해도 무방 — 나중에 만들 페이지를 예약하는 것

---

## 품질 원칙

- **명확성 우선**: 모호한 표현 금지. 팀원도, LLM도 이 위키를 읽는다.
- **"왜"를 항상 포함**: 정의만이 아니라 왜 중요한지, 왜 이렇게 동작하는지. 이유 없는 규칙은 다음 사람이 "이거 왜 이렇게 되어있지?"라며 리팩터링해버린다.
- **연결 중심**: 단독 페이지보다 다른 페이지와의 관계가 더 중요.
- **현재 시제 + 코드가 우선**: 코드와 위키가 어긋나면 코드가 맞다. 위키를 갱신한다.
- **모순은 삭제가 아니라 표시**: 기존 내용과 충돌하면 `> ⚠️ 모순: ...`으로 표시.
- **작성자를 남긴다**: 여러 명이 쓰는 위키이므로 `log.md`와 `decisions/`의 `author` 필드는 생략하지 않는다.

---

## 세션 시작 체크리스트

새 Claude Code 세션을 시작하면:

1. `git pull` — 다른 팀원이 갱신한 내용을 먼저 받는다
2. `CLAUDE.md` 읽기 (이 파일)
3. `wiki/index.md` 읽기 — 현재 위키 상태 파악
4. `wiki/log.md` 마지막 5개 항목 읽기 — 최근 작업 파악
5. 사용자 요청 처리
6. 위키를 갱신했다면 세션 끝에 `git add` → `commit` → `push` (또는 사용자에게 확인 후)
